1) Em um elemento formulário qual a finalidade do atributo action e method explique se precisar com
gráficos.
R: Em um elemento formulário o atributo "action" define a URL em que os dados vão ser enviados após seu envio. Já o atributo "method" determina como isso irá acontecer usando o método HTTP, podendo utilizar "GET" ou "POST" que são seus atributos principais.
GET: obter informações do servidor de forma simples e rápida, mas evite enviar dados sensíveis. 
POST:  para enviar informações ao servidor de forma mais segura e eficiente, especialmente para dados confidenciais ou extensos.

2) Crie uma página HTML com campos de input de dados de diferentes tipos e um paragrafo antes de cada
tipos de campo informando a finalidade do tipos e principais atributos.
R:
<!DOCTYPE html>
 <!DOCTYPE html>
 <html lang="en">
 <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
 </head>
 <body>

    <form action="" method="POST">
        <fieldset>

O tipo "EMAIL" serve para a entrada de endereços de emails. Name, type e required são os principais atributos desse tipo;

            <label for="inputEmail">Email:</label>
            <input type="email" id="inputEmail" name="inputEmail">
                <br>

O tipo "PASSWORD" serve para a entrada de senhas. Name, type, id, maxlength e required são os principais atributos desse tipo:

            <label for="password"> Senha:</label>
            <input type="password" id="password" name="password">
                <br>

O tipo "FILE" tem a finalidade de carregar arquivos. accept (Aceitar Tipos de Arquivo), multiple (Múltiplos Arquivos) e required são os principais atributos desse tipo;

            <label for="file">Arquivo:</label>
            <input type="file" id="file" name="file">
            <br>

        </fieldset>
            <input type="submit";
    </form>

 </body>
 </html>

3) O que diferencia um <button> de uma <input type=”button”> de um <input type=”submit”>
R:
Todos são usados para criar botôes, porém tem utilidades específicas:
<button>: Cria um botão que pode ter texto e outros elementos dentro. Usa-se quando o intuito é estilizar-lo mais facilmente.
<input type="button">: Cria um botão simples.
<input type="submit">: Se usa dentro de um formulário para enviar os dados do formulário para o servidor quando o botão é clicado.

4) Qual a tag para criação de links documentos HTML internos e externos? Como podemos utilizar para
referenciar um documento na web?
R:
 
A tag <a> é tag utilizada para a criação de links documentos HTML, tanto internos quanto externos.
Para link interno: <a href="blabla-blabla.html">Clique aqui para a blabla-blabla</a>
Para link externo: <a href="https://www.blablabla.com/artigo">Clique aqui para o Artigo</a>
 
5) O que o trecho de código abaixo faz?
R:  O código exibe um título grande e está referenciando uma imagem e mostrando as coordenadas dos 3 elementos no mapa, cada um com um formato, coordenada, nome e link.
